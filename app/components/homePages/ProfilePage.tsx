import UploadImageButton from "../uploads/UploadImageButton";

const ProfilePage = () => {
    return (
        <div>
            <h1>Change your profile image</h1>
            <UploadImageButton />
        </div>
    );
};

export default ProfilePage;
